/** TODO: Corinne - Saturday project implment collections so we don't need to manange the list and view better */
/* Inspired from http://liquidmedia.org/blog/2011/02/backbone-js-part-3/ */
/*
var UploadedFiles = Backbone.Collection.extend({
  model: UploadedFile,
  initialize : function() {
    var that = this;
    this._uploadedFileViews = [];
   },
 
  render : function() {
    var that = this;
    // Clear out this element.
    $(this.el).empty();
 
    // Render each sub-view and append it to the parent view's element.
    _(this._uploadedFilesViews).each(function(dv) {
      $(that.el).append(dv.render().el);
    });
  }
});
*/